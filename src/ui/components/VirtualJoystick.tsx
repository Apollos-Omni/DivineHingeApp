import React from "react";
import { View } from "react-native";
type Props = { onMove?: (dx: number, dy: number) => void };
export default function VirtualJoystick(_props: Props) {
  return <View />;
}
