# === OpenAI ===
OPENAI_API_KEY=

# Optional: enable MCP with OpenAI Responses (remote MCP servers)
MCP_GITHUB_BASE_URL= # e.g. https://mcp.github.com (managed) or your self-host
MCP_GITHUB_ACCESS_TOKEN= # short-lived OAuth token or PAT if self-hosting
MCP_SUPABASE_SERVER_URL=
MCP_SUPABASE_API_KEY=

# === GitHub ===
GITHUB_TOKEN= # 1) Download the zip from our chat, then:
$zip = "$HOME\Downloads\ai-orchestrator-starter.zip"   # adjust if you saved elsewhere
$dest = "C:\users\phelp\documents\divinehingeapp"
Expand-Archive -Path $zip -DestinationPath $dest -Force

# 2) Optional but recommended: commit the new folders
cd $dest
git add ai .github supabase
git commit -m "Add AI Orchestrator starter (ai/, CI, supabase function)"

GITHUB_REPO_OWNER=your-user-or-org
GITHUB_REPO_NAME=your-repo

# === Supabase ===
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE= # required for server-side task queue use

# === Orchestrator ===
REPO_LOCAL_PATH=../ # path to the repo on disk for local runs
DEFAULT_BRANCH=main
